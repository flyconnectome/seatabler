% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{seatable_list_selected}
\alias{seatable_list_selected}
\title{Select rows from a SeaTable table by id}
\usage{
seatable_list_selected(
  ids = NULL,
  table,
  fields = "*",
  idfield = NULL,
  base = NULL,
  con = default_connection(),
  ...
)
}
\arguments{
\item{ids}{Ids to look up, matched against \code{idfield}. \code{NULL} (the default)
returns every row.}

\item{table}{Name of the table.}

\item{fields}{Columns to return: \code{"*"} (default) for all, a comma-separated
string, or a character vector of column names (back-quoted for you).}

\item{idfield}{Column to match \code{ids} against. Required when \code{ids} is
supplied.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{...}{Passed to \code{\link[=seatable_query]{seatable_query()}} (e.g. \code{limit}, \code{python}).}
}
\value{
A data.frame of the selected rows and columns.
}
\description{
Builds and runs a \verb{SELECT ... WHERE <idfield> IN (...)} query
for a set of ids -- a convenience over writing the SQL by hand. With no
\code{ids} it returns the whole table.
}
\details{
This is a generic id lookup: \code{ids} are matched against \code{idfield}
verbatim, with no coercion. Consumer packages that key on a domain id (for
example a flywire root id that must be resolved to its current value first)
should do that resolution in their own wrapper before calling this.
Whether values are quoted in the SQL is decided from \code{idfield}'s column
type (numeric columns are left unquoted).
}
\seealso{
\code{\link[=seatable_query]{seatable_query()}}, \code{\link[=seatable_list_rows]{seatable_list_rows()}}
}
