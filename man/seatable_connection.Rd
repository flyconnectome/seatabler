% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{seatable_connection}
\alias{seatable_connection}
\alias{is_seatable_connection}
\title{Create a connection to a SeaTable server}
\usage{
seatable_connection(
  url,
  token = NULL,
  token_envvar = getOption("seatabler.token_envvar", "SEATABLE_TOKEN"),
  workspace_id = NULL,
  cachedir = getOption("seatabler.cachedir", NULL),
  name = NULL
)

is_seatable_connection(x)
}
\arguments{
\item{url}{Base URL of the SeaTable server (e.g.
\code{"https://cloud.seatable.io/"}). Required — there is no default server.}

\item{token}{Optional API token string embedded directly in the connection.
When supplied it takes precedence over \code{token_envvar}. It is redacted by the
print method, but is still an ordinary list element — do not commit
connections carrying an embedded token to shared repositories.}

\item{token_envvar}{Name of the environment variable holding the API token,
used when \code{token} is not supplied. Defaults to the \code{seatabler.token_envvar}
option, or \code{"SEATABLE_TOKEN"}.}

\item{workspace_id}{Optional workspace id (character or numeric). When \code{NULL}
it is discovered from the base name where possible.}

\item{cachedir}{Optional per-connection cache directory. When \code{NULL} the
\code{seatabler.cachedir} option is used.}

\item{name}{Optional short human-readable label for the connection.}

\item{x}{An object to test / print.}
}
\value{
An object of class \code{seatable_connection}.
}
\description{
A \code{seatable_connection} captures everything needed to reach a
particular SeaTable server: its URL, the environment variable holding your
API token, and (optionally) the workspace id and cache directory. It is the
single place server configuration lives, so downstream functions and
wrapper packages can bind one connection rather than passing
URL/token/workspace arguments around.
}
\details{
By default the token itself is not stored in the object — only the
name of the environment variable that holds it (\code{token_envvar}), resolved
fresh at call time by \code{\link[=seatable_token]{seatable_token()}}. You may instead embed a literal
\code{token}, which is convenient when talking to several servers (each needs
its own token); the print method redacts it.
}
\examples{
con <- seatable_connection(
  url = "https://cloud.seatable.io/",
  token_envvar = "BANCTABLE_TOKEN",
  workspace_id = "57832")
con
}
