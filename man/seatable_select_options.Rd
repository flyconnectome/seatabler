% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{seatable_select_options}
\alias{seatable_select_options}
\alias{seatable_add_select_options}
\title{Read and add options for a select column}
\usage{
seatable_select_options(
  table,
  col = NULL,
  base = NULL,
  con = default_connection()
)

seatable_add_select_options(
  table,
  col,
  options,
  base = NULL,
  con = default_connection()
)
}
\arguments{
\item{table}{Name of the table.}

\item{col}{For \code{seatable_select_options}, single- or multiple-select column
name(s); \code{NULL} (the default) returns every such column. For
\code{seatable_add_select_options}, a single column name.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{options}{Character vector of new option name(s) to add.}
}
\value{
For \code{seatable_select_options}, a named list of character vectors (one
per column) of option names. For \code{seatable_add_select_options}, the
SeaTable response, invisibly.
}
\description{
\code{seatable_select_options} returns the option names currently
defined for one or more single- or multiple-select columns.
\code{seatable_add_select_options} adds new options to a column's vocabulary.
}
\examples{
\dontrun{
seatable_select_options("neurons", "status", con = con)
seatable_add_select_options("neurons", "status", "reviewed", con = con)
}
}
