% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rest.R
\name{seatable_rest}
\alias{seatable_rest}
\alias{seatable_base_rest}
\title{Call a SeaTable REST endpoint directly}
\usage{
seatable_rest(
  path,
  con = default_connection(),
  method = NULL,
  body = NULL,
  query = NULL,
  retries = 3L,
  parse = TRUE
)

seatable_base_rest(
  path,
  base,
  con = default_connection(),
  method = NULL,
  body = NULL,
  query = NULL,
  retries = 3L,
  parse = TRUE
)
}
\arguments{
\item{path}{Endpoint path relative to the server root (\code{seatable_rest()}) or
to \verb{api-gateway/api/v2/dtables/<base-uuid>/} (\code{seatable_base_rest()}).}

\item{con}{A \link{seatable_connection}.}

\item{method}{HTTP method. Defaults to \code{"GET"}, or \code{"POST"} when a \code{body} is
supplied.}

\item{body}{A list encoded as the JSON request body, or \code{NULL}.}

\item{query}{A named list of URL query parameters, or \code{NULL}.}

\item{retries}{Number of times to retry a 429 or 5xx response.}

\item{parse}{Whether to parse the JSON response (default) or return the raw
\code{httr2} response object.}

\item{base}{A \code{Base} object from \code{\link[=seatable_base]{seatable_base()}}, or a base name.}
}
\value{
The parsed JSON response as an R list, or an \code{httr2_response} when
\code{parse = FALSE}. Endpoints with an empty body return \code{NULL}.
}
\description{
Low-level access to SeaTable's HTTP API, for endpoints the
Python SDK does not expose. \code{seatable_rest()} talks to the \emph{account} API
(paths under \verb{api/v2.1/}, authenticated with the connection's API token);
\code{seatable_base_rest()} talks to a \emph{base} through the API gateway (paths
under \verb{api-gateway/api/v2/dtables/<base-uuid>/}, authenticated with that
base's JWT).
}
\details{
These are the transport behind \code{\link[=seatable_archive_rows]{seatable_archive_rows()}},
\code{\link[=seatable_unarchive_rows]{seatable_unarchive_rows()}} and \code{\link[=seatable_snapshots]{seatable_snapshots()}}, and are exported so
that consumer packages can reach endpoints seatabler does not yet wrap
without reimplementing authentication.

Two details are easy to get wrong and are handled here. Requests are forced
onto HTTP/1.1 (curl's \code{CURL_HTTP_VERSION_1_1}, numeric value 2), because
SeaTable's gateway can return HTTP/2 framing errors under load. And JSON
bodies encode \code{NA} as \code{null} rather than the string \code{"NA"}, which SeaTable
rejects in number columns.

Rate-limited (429) and server-error (5xx) responses are retried with
exponential backoff; other error statuses raise an error carrying
SeaTable's own \code{error_message} where it supplies one.
}
\examples{
\dontrun{
con <- seatable_connection("https://cloud.seatable.io/", token_envvar = "MY_TOKEN")

# An account endpoint: which bases can I see?
seatable_rest("api/v2.1/dtables/", con = con)

# A base endpoint, authenticated with the base JWT
base <- seatable_base("my_base", con = con)
seatable_base_rest("metadata/", base = base, con = con)
}
}
