% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots.R
\name{seatable_snapshots}
\alias{seatable_snapshots}
\title{List the snapshots of a SeaTable base}
\usage{
seatable_snapshots(base_name, con = default_connection(), workspace_id = NULL)
}
\arguments{
\item{base_name}{Name of the base.}

\item{con}{A \link{seatable_connection}.}

\item{workspace_id}{Workspace holding the base. Defaults to the connection's
\code{workspace_id}, or is looked up with \code{\link[=seatable_workspace_id]{seatable_workspace_id()}}.}
}
\value{
A data frame of snapshots, one row each, typically with the snapshot
name and its creation time. An empty data frame when the base has none.
}
\description{
SeaTable snapshots a base periodically, and a snapshot is the
only way back from a bad bulk update. This lists the snapshots available
for a base, so you can see how far back you can go and pick the one to
restore from in the web interface.
}
\details{
Restoring is deliberately not wrapped: it is destructive, has no
undo, and belongs in the SeaTable UI where it asks for confirmation.
}
\examples{
\dontrun{
seatable_snapshots("my_base", con = con)
}
}
