% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{seatable_delete_column}
\alias{seatable_delete_column}
\title{Delete a column from a SeaTable table}
\usage{
seatable_delete_column(
  table,
  column_key,
  base = NULL,
  con = default_connection()
)
}
\arguments{
\item{table}{Name of the table.}

\item{column_key}{Internal key of the column to delete (e.g. \code{"8blF"}), from
\code{seatable_columns(table, include_key = TRUE)}.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}
}
\value{
The SeaTable response, invisibly.
}
\description{
Removes a column and all the data in it. This cannot be undone,
so \code{seatable_delete_column()} takes the internal column \emph{key} rather than
its name: looking the key up with
\code{seatable_columns(table, include_key = TRUE)} is a deliberate speed bump.
}
\examples{
\dontrun{
cols <- seatable_columns("neurons", include_key = TRUE, con = con)
key <- cols$key[cols$name == "scratch"]
seatable_delete_column("neurons", key, con = con)
}
}
