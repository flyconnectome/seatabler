% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{seatable_query}
\alias{seatable_query}
\title{Run a SQL query against a SeaTable server}
\usage{
seatable_query(
  sql,
  con = default_connection(),
  limit = 100000L,
  base = NULL,
  python = FALSE,
  convert = TRUE,
  paginate = TRUE,
  chunksize = NULL
)
}
\arguments{
\item{sql}{A single SQL \code{SELECT} statement. The table name is parsed from the
\code{FROM} clause.}

\item{con}{A \link{seatable_connection}.}

\item{limit}{Maximum rows to return. The default fetches all rows,
paginating as needed.}

\item{base}{Optional base name or \code{Base} object; discovered from the table
name when \code{NULL}.}

\item{python}{If \code{TRUE}, return the raw pandas \code{DataFrame} instead of an R
\code{data.frame}.}

\item{convert}{Passed to the SeaTable Python \code{query} call.}

\item{paginate}{Whether to page through large results automatically
(default \code{TRUE}). SeaTable's SQL endpoint silently caps a single call at a
server-specific maximum; pagination returns the full result.}

\item{chunksize}{Advanced: force \code{LIMIT}/\code{OFFSET} paging in fixed windows of
this size. The default \code{NULL} auto-detects the server's per-call cap.}
}
\value{
An R \code{data.frame} (or a pandas \code{DataFrame} when \code{python = TRUE}).
}
\description{
Run a SQL query against a SeaTable server
}
\examples{
\dontrun{
con <- seatable_connection("https://cloud.seatable.io/", "BANCTABLE_TOKEN",
                           workspace_id = "57832")
seatable_query("SELECT * FROM banc_meta", con = con)
}
}
