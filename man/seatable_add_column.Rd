% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{seatable_add_column}
\alias{seatable_add_column}
\alias{seatable_add_columns}
\title{Add columns to a SeaTable table}
\usage{
seatable_add_column(
  table,
  column_name,
  column_type = "text",
  base = NULL,
  con = default_connection(),
  column_data = NULL,
  column_key = NULL
)

seatable_add_columns(
  table,
  columns,
  base = NULL,
  con = default_connection(),
  progress = TRUE
)
}
\arguments{
\item{table}{Name of the table to add to.}

\item{column_name}{Name of the new column.}

\item{column_type}{SeaTable column type (see Details). Defaults to \code{"text"}.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{column_data}{Optional list of type-specific column settings, e.g. the
\code{options} of a select column.}

\item{column_key}{Optional key of an existing column, to position the new
column immediately after it.}

\item{columns}{A data frame with \code{name} and \code{type} columns — the same shape
\code{\link[=seatable_columns]{seatable_columns()}} returns, so the schema of one table can be replayed
onto another.}

\item{progress}{Whether to report each column as it is added.}
}
\value{
For \code{seatable_add_column()}, the new column's definition, invisibly.
For \code{seatable_add_columns()}, the subset of \code{columns} that was actually
added, invisibly.
}
\description{
\code{seatable_add_column()} inserts a single new column;
\code{seatable_add_columns()} adds several at once from a data frame of names
and types, skipping any that already exist. Together they let a migration
script bring a table's schema up to date without touching the SeaTable web
interface.
}
\details{
\code{column_type} uses SeaTable's own vocabulary — most commonly
\code{"text"}, \code{"long-text"}, \code{"number"}, \code{"date"}, \code{"checkbox"},
\code{"single-select"} or \code{"multiple-select"}; see
\url{https://api.seatable.io/reference/insert-column-2} for the full list.

Options for a select column, and other type-specific settings, go in
\code{column_data}, e.g. \code{column_data = list(options = list(list(name = "yes"), list(name = "no")))}.
}
\examples{
\dontrun{
seatable_add_column("neurons", "cell_type", con = con)
seatable_add_column("neurons", "n_synapses", "number", con = con)

# Bring a table up to a target schema, adding only what is missing
seatable_add_columns("neurons",
  data.frame(name = c("side", "notes"), type = c("text", "long-text")),
  con = con)
}
}
