% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bigdata.R
\name{seatable_archive_rows}
\alias{seatable_archive_rows}
\alias{seatable_unarchive_rows}
\title{Move rows into and out of SeaTable big data storage}
\usage{
seatable_archive_rows(
  table,
  view_name = NULL,
  view_id = NULL,
  base = NULL,
  con = default_connection()
)

seatable_unarchive_rows(
  table,
  row_ids,
  base = NULL,
  con = default_connection()
)
}
\arguments{
\item{table}{Name of the table.}

\item{view_name}{Name of the view whose rows should be archived. Supply
exactly one of \code{view_name} or \code{view_id}.}

\item{view_id}{Id of the view whose rows should be archived.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{row_ids}{Character vector of row ids (the \verb{_id} column) to unarchive.}
}
\value{
The SeaTable response, invisibly.
}
\description{
SeaTable caps the number of rows a base holds in its ordinary
grid; archiving moves rows into "big data" storage, which is much larger
and still queryable with \code{\link[=seatable_query]{seatable_query()}}, at the cost of no longer being
editable in the web interface. \code{seatable_archive_rows()} moves rows out of
the grid, \code{seatable_unarchive_rows()} brings them back.
}
\details{
The two directions select rows differently, because the API does.
Archiving takes a \strong{view}: make a view holding exactly the rows you want
archived (a filtered view is the usual way) and name it. Earlier versions
of the endpoint accepted a SQL \code{WHERE} clause; that is gone, and a view is
now the only way to choose rows. Unarchiving instead takes explicit
\code{row_ids}, which you can get by querying the archived rows with
\code{seatable_query("SELECT _id FROM ...")}.

Archiving is not easily undone in bulk — you need the row ids to bring rows
back — so check the view holds what you expect before calling.
}
\examples{
\dontrun{
# Archive everything in the (filtered) view called "to archive"
seatable_archive_rows("neurons", view_name = "to archive", con = con)

# ...and bring two rows back
seatable_unarchive_rows("neurons",
  row_ids = c("FoDxhChYQSycLm88JZ11RA", "Yb3kQ0rRQ0aXk3lWk0mWLQ"),
  con = con)
}
}
