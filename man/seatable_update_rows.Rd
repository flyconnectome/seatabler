% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{seatable_update_rows}
\alias{seatable_update_rows}
\alias{seatable_append_rows}
\title{Update or append rows in a SeaTable table}
\usage{
seatable_update_rows(
  df,
  table,
  base = NULL,
  con = default_connection(),
  append_allowed = TRUE,
  chunksize = 1000L,
  multi_select_cols = NULL,
  allow_new_options = FALSE
)

seatable_append_rows(
  df,
  table,
  base = NULL,
  con = default_connection(),
  bigdata = FALSE,
  chunksize = 1000L,
  multi_select_cols = NULL,
  allow_new_options = FALSE
)
}
\arguments{
\item{df}{A data.frame of rows to write. For updates it must carry an \verb{_id}
or \code{row_id} column identifying each remote row.}

\item{table}{Name of the table.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{append_allowed}{For \code{seatable_update_rows}, whether rows whose id is
missing may be appended as new rows rather than raising an error.}

\item{chunksize}{Split large requests into calls of at most this many rows.}

\item{multi_select_cols}{Column names to treat as multiple-select
(list-per-cell). The default \code{NULL} auto-detects them from column metadata.}

\item{allow_new_options}{When a multiple-select value is not already an
option for its column, whether to add it automatically (via
\code{\link[=seatable_add_select_options]{seatable_add_select_options()}}) rather than erroring.}

\item{bigdata}{For \code{seatable_append_rows}, whether to write straight into big
data (archived) storage via REST rather than the ordinary grid.}
}
\value{
Logical, invisibly: \code{TRUE} on success. Failures normally abort with
the SeaTable error message.
}
\description{
\code{seatable_update_rows} updates existing rows (matched by their
\verb{_id}/\code{row_id}), returning \code{TRUE} on success. \code{seatable_append_rows} adds
new rows, optionally straight into big data storage.
}
\details{
SeaTable keeps a unique \verb{_id} for every row, returned by
\code{\link[=seatable_query]{seatable_query()}}. To update, keep that column (named \verb{_id} or \code{row_id})
in \code{df}; to append you do not need it (and are warned if you supply one).
Writes are chunked at \code{chunksize} rows because the API caps a single call
at 1000.

\strong{Multiple-select columns} need care: SeaTable expects a genuine list of
option names per cell, and silently invents a new option from any bare
string it is handed instead. Any column SeaTable reports as
\code{"multiple-select"} is auto-detected and routed through a list-per-cell
path; pass \code{multi_select_cols} to override detection. A scalar cell
(\code{"AB"}, or comma-joined \code{"AB,CD"}) is accepted as shorthand and split on
commas — symmetric with how such a cell reads back — or supply a
list-column directly (\code{I(list(c("AB", "CD")))}), which also lets you write
an option name that itself contains a comma. Every resulting value is
checked against the column's existing vocabulary and rejected unless it is
already an option; see \code{allow_new_options} and \code{\link[=seatable_add_select_options]{seatable_add_select_options()}}.

\strong{Big data} (\code{bigdata = TRUE}, append only): rows go through the
\code{add-archived-rows} REST endpoint straight into archived storage, rather
than the ordinary grid via the SDK. This is for bulk loads that would
exceed the base's grid row cap; the base must have big data enabled or the
gateway returns an error. Big data is an append \emph{destination}, not a
property of the table (a table can hold both grid and archived rows) — see
\code{\link[=seatable_archive_rows]{seatable_archive_rows()}}.
}
\examples{
\dontrun{
rows <- seatable_query("SELECT _id, name FROM neurons LIMIT 2", con = con)
rows$name <- toupper(rows$name)
seatable_update_rows(rows, "neurons", con = con)

seatable_append_rows(data.frame(name = "new cell"), "neurons", con = con)
}
}
\seealso{
\code{\link[=seatable_query]{seatable_query()}} to read rows back, \code{\link[=seatable_archive_rows]{seatable_archive_rows()}} to
move rows into and out of big data storage.
}
