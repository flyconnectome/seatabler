% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{seatable_login}
\alias{seatable_login}
\alias{seatable_generate_token}
\title{Authenticate with a SeaTable server}
\usage{
seatable_login(con = default_connection())

seatable_generate_token(user, pwd, con = default_connection(), persist = TRUE)
}
\arguments{
\item{con}{A \link{seatable_connection}.}

\item{user, pwd}{Your SeaTable user name (email) and password, used only to
mint a token.}

\item{persist}{Whether to persist the token to \verb{~/.Renviron} so it is
available in future R sessions (default \code{TRUE}). Any existing line for the
connection's \code{token_envvar} is replaced, otherwise a new line is appended,
and the token is set for the current session too. With \code{persist = FALSE}
nothing is written or set — the token is only returned.}
}
\value{
For \code{seatable_login}, a Python
\href{https://seatable.github.io/seatable-scripts/python/account/}{\code{Account}}
object (reticulate-wrapped), invisibly. For \code{seatable_generate_token}, the
token string, invisibly.
}
\description{
\code{seatable_login} opens an authenticated session using the
connection's API token. \code{seatable_generate_token} mints a fresh permanent
user-level token from your user name and password, makes it available for
the current session and, by default, persists it to \verb{~/.Renviron} for
future sessions.
}
\details{
Authentication is by API token, never by password at call time:
\code{seatable_login} constructs a Python \code{Account} with no credentials and sets
its token directly, so SeaTable uses that token for every subsequent call.
Call \code{seatable_generate_token} once to obtain a token; thereafter the token
in the connection's \code{token_envvar} (or embedded in the connection object) is
all that is needed.

Tokens are per-server, so give each server its own \code{token_envvar} name
(wrapper packages typically pin one, e.g. \code{"FLYTABLE_TOKEN"}). With
\code{persist = TRUE} the token is written to \verb{~/.Renviron} under that name,
replacing any existing line for it.
}
\examples{
\dontrun{
con <- seatable_connection("https://cloud.seatable.io/",
                           token_envvar = "BANCTABLE_TOKEN")
seatable_generate_token("me@example.com", "secret", con = con)
seatable_login(con)
}
}
