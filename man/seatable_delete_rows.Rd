% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{seatable_delete_rows}
\alias{seatable_delete_rows}
\title{Delete rows from a SeaTable table}
\usage{
seatable_delete_rows(
  ids,
  table,
  base = NULL,
  con = default_connection(),
  DryRun = TRUE
)
}
\arguments{
\item{ids}{A character vector of row \verb{_id}s, or a data.frame with an \verb{_id}
column (as returned by \code{\link[=seatable_query]{seatable_query()}}). Duplicates are ignored.}

\item{table}{Name of the table.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{DryRun}{When \code{TRUE} (the default) no rows are deleted; the ids that
would be deleted are returned. Pass \code{FALSE} to actually delete.}
}
\value{
With \code{DryRun = TRUE}, the character vector of ids. With
\code{DryRun = FALSE}, the number of rows the server reports as deleted (a
warning is raised if that is fewer than requested).
}
\description{
Deletes rows by their SeaTable \verb{_id}. Defaults to a dry run that
returns the ids it \emph{would} delete without touching the server, so you can
check the selection first.
}
\seealso{
\code{\link[=seatable_query]{seatable_query()}}, \code{\link[=seatable_update_rows]{seatable_update_rows()}}
}
