% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{seatable_list_rows}
\alias{seatable_list_rows}
\title{List rows from a SeaTable table}
\usage{
seatable_list_rows(
  table,
  base = NULL,
  con = default_connection(),
  view_name = NULL,
  order_by = NULL,
  desc = FALSE,
  start = 0L,
  limit = Inf,
  python = FALSE,
  chunksize = NULL
)
}
\arguments{
\item{table}{Name of the table.}

\item{base}{Optional base name or \code{Base} object; discovered from \code{table} when
\code{NULL}.}

\item{con}{A \link{seatable_connection}.}

\item{view_name}{Optional view limiting the rows/columns returned.}

\item{order_by}{Optional column name to order by.}

\item{desc}{Whether to sort descending (default ascending).}

\item{start}{Row offset to start from (0-based).}

\item{limit}{Maximum rows to return; the default \code{Inf} returns all rows.}

\item{python}{Whether to return the pandas \code{DataFrame} rather than an R
\code{data.frame}.}

\item{chunksize}{Advanced: rows to request per call. The default \code{NULL}
chooses a size from the column count (SeaTable allows roughly one million
cells per request).}
}
\value{
An R \code{data.frame}, or a pandas \code{DataFrame} when \code{python = TRUE}.
}
\description{
Reads rows through SeaTable's \code{list_rows} API (rather than SQL),
optionally restricted to a view and ordered by a column. Returns an R
\code{data.frame}, or the raw pandas \code{DataFrame} when \code{python = TRUE}.
}
\details{
The \code{list_rows} endpoint caps the number of rows per request, and
that cap is server-configurable with no API to discover it, so a page
coming back shorter than requested does \strong{not} signal the end of the
table -- only an empty page does. Rows are therefore fetched in chunks
until an empty page (or \code{limit}) is reached, then concatenated once and
coerced together, so multiple-select and other list columns flatten
uniformly across chunks.
}
\seealso{
\code{\link[=seatable_query]{seatable_query()}}, \code{\link[=seatable_list_selected]{seatable_list_selected()}}
}
